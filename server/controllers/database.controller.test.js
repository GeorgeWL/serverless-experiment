describe('Name of the group', () => {
  it('should get a device when provided with an id', () => {

  });
  it('should get device with timestamps if provided with id and options.timestamps', () => {

  })
});
